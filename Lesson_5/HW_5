Задача 1. Создать функцию mul, принимающую два числа и
возвращающую их произведение

Задача 2. Создать функцию print_matrix, которая принимает один
параметр - матрицу m на n и выводит ее на экран

Задача 3. Написать анонимную функцию, которая вычисляет значение
выражения x ** 2 - x^1/2 + 3 * x - 4 * x ** 3 + 2

Задача 4. Написать рекурсивную функцию, которая выводит
последовательность n первых чисел Фибоначчи

Задача 5. Рекурсивный алгоритм f(n) задан следующим образом:
f(n) = 0, при n <= 1
f(n) = f(n - 1) + 3 * n^2, если n > 1 и при этом нечетно
f(n) = n / 2 + f(n - 1) + 2, если n > 1 и при этом четно

Чему равно f(49). Написать рекурсивную функцию, расписать
несколько первых шагов стека вызовов.